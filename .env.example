# Prisma — SQLite for local dev (file path is relative to prisma/schema.prisma).
# Swap for a PostgreSQL connection string when ready for production.
DATABASE_URL="file:./dev.db"

# NextAuth.js v5 — generate with `openssl rand -base64 32`
AUTH_SECRET=""
